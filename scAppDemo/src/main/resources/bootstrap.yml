# this is demo project
spring:
  cloud:
    config:
      failFast: true
      name: neo-config
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: config-server